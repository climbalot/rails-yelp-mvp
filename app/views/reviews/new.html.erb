<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, as: :select, collection: [0, 1, 2, 3, 4, 5], prompt: 'Choose a rating' %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>